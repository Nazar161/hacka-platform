import React, { FC } from 'react';

const Vacancies: FC = () => {
    return (
        <div>
            <p>Vacancies Page</p>
            <p>captain ? button+modal(for creating new vacancy) : nothing(just vacancies)</p>
            <p>filtering vacancies by skills</p>
            <p>radio button or some filtering for displaying own team's vacancies</p>
        </div>
    );
};

export default Vacancies;