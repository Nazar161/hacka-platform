import React, { FC } from 'react';

const NotFound: FC = () => {
    return (
        <main style={{ padding: "1rem" }}>
            <p>There's nothing here!</p>
        </main>
    );
};

export default NotFound;